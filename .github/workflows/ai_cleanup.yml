name: "AI Cleanup 🧹"


on:
  pull_request:
    types: [closed]
  issues:
    types: [closed]


jobs:
  call:
    uses: codex-k8s/codexctl/.github/workflows/ai_cleanup.yml@main
    secrets: inherit
