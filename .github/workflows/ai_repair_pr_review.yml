name: "AI Staging Repair PR Review 👁"


on:
  pull_request_review:
    types: [submitted]


jobs:
  call:
    uses: codex-k8s/codexctl/.github/workflows/ai_repair_pr_review.yml@main
    secrets: inherit
