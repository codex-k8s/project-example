apiVersion: v1
kind: Service
metadata:
  name: telegram-executor
  namespace: codex-system
spec:
  selector:
    app: telegram-executor
  ports:
    - name: http
      port: 8080
      targetPort: http
