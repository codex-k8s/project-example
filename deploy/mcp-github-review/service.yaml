apiVersion: v1
kind: Service
metadata:
  name: yaml-mcp-github-review
  namespace: codex-system
spec:
  selector:
    app: yaml-mcp-github-review
  ports:
    - name: http
      port: 8080
      targetPort: http
