apiVersion: v1
kind: Service
metadata:
  name: yaml-mcp-secrets-postgres-k8s
  namespace: codex-system
spec:
  selector:
    app: yaml-mcp-secrets-postgres-k8s
  ports:
    - name: http
      port: 8080
      targetPort: http
